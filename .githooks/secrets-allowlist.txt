# Rutas exentas de los patrones de nivel 2 (credenciales en texto plano y JWT).
# Los bloqueos de nivel 1 (.env, claves privadas, credenciales de nube y cadenas
# de conexion) NO se pueden eximir desde aqui.
#
# Una linea = una expresion regular contra la ruta relativa del archivo.
# Cada entrada debe justificar por que su contenido es ficticio.

# Pruebas: usuarios y tokens sinteticos por diseno.
^src/.*\.(test|spec)\.(ts|tsx)$
^src/test/

# Documentacion de diseno: capturas y HTML del prototipo Stitch.
^docs/

# Este propio detector y su lista, que contienen los patrones como texto.
^\.githooks/

# README: documenta los NOMBRES de las variables de entorno, nunca sus valores.
^README\.md$
